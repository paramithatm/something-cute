import 'package:flutter/material.dart';

class Collection extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
        title: "Something",
        home: Scaffold(
          backgroundColor: Colors.blue[200],
          body: Text("something cute"),
        ));
  }
}
